openai>=1.40
httpx>=0.27
# Streaming STT/TTS (stt.py's transcribe(), tts.py's synthesize_stream()) —
# Sarvam doesn't publish a raw WebSocket protocol, only this SDK's
# AsyncSarvamAI client (speech_to_text_streaming/text_to_speech_streaming).
sarvamai>=0.1.8
aiohttp
psycopg[binary,pool]>=3.2
python-dotenv
boto3
fastapi>=0.110
uvicorn[standard]>=0.29
pyjwt>=2.8
# Only needed if an agent's voice is an ElevenLabs one (MP3 TTS output) —
# see audio.py's _decode_mp3. Requires ffmpeg on PATH.
pydub>=0.25
google-cloud-texttospeech>=2.31.0
# Anti-aliasing low-pass filter before downsampling TTS output to 8kHz for
# the phone leg (audioop.ratecv alone has no filtering) — see audio.py.
numpy>=1.26
